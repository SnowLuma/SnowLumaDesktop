export const APP_NAME = 'SnowLumaDesktop' as const;
// User-facing display name. Kept as a single token now (was "SnowLuma
// Desktop") so the window title, Task Manager entry, NSIS shortcut, and
// brand string all match.
export const APP_DISPLAY_NAME = 'SnowLumaDesktop' as const;
